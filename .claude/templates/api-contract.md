# API/event contract: [Operation]

- **Related plan/ticket:**
- **Owner:**
- **Consumers affected:** name them from the *Consumers* table in `CLAUDE.md`

## Endpoint or event

- Type: REST | Webhook | Queued job | Scheduled command
- Route (`routes/api.php`):
- HTTP method:
- Middleware: `auth:api` / public
- Permission or Gate:
- Route id constraint: `->where('xId', config('custom.numeric_regex'))`
- Rate limiter:
- Idempotency key:
- Activity-log action:

## Request

Form Request class (extends `BaseRequest`):

| Field | Rule | Required/nullable | Normalisation | Authoritative source |
|---|---|---|---|---|
| | | | | client / server / derived |

Parsed through `BaseRequest` helpers — `bigDecimal()`, `arrayIds()`, `enum()`,
`boolean()`, `getRelations()`, `getMetaData()` — never raw `$request->input()`.

Authoritative money, price, totals, role, permission, owner, entitlement, and
approval fields must be server-derived.

## Data objects

- `XData` fields:
- `XFilterData` fields:
- `MetaData` usage: relations · columns · search · sortField/sortDirection ·
  page/perPage/offset · groupBy · filters

## Response

- API Resource class:
- Wrapped by: `ResponseUtil::resource(...)` / `ResponseUtil::success('…')`
- Fields exposed:
- Fields deliberately withheld:

```json
{}
```

Never return a raw model or query result.

## Errors

Every failure keeps one envelope:

```json
{
  "success": false,
  "message": "Safe message"
}
```

| HTTP | Thrown by | Condition | Enumeration behaviour | Retryable |
|---|---|---|---|---|
| 400 | `BadRequestException` | | | |
| 401 | `auth:api` | | | |
| 422 | validation / `ProcessingException` | | | |

Clients program against the status and a stable message. Do not introduce a
second error shape.

## Pagination / filter / sort

- built from `MetaData`:
- maximum `perPage`:
- stable sort field and tie-breaker:
- searchable fields:
- ownership predicate applied in the repository:
- no unpaginated path:

## OpenAPI document

Generated by `dedoc/scramble` from the Form Request, the Resource, and the route
middleware — nothing here is hand-written. Confirm after `php artisan scramble:export`:

- operation appears under the expected path and method:
- marked `security: []` (public) or bearer-secured — and is that correct?
- request fields present and correctly required/nullable:
- response schema matches the Resource:
- if a field is missing, the defect is in `rules()` or the Resource, not in the
  documentation:

## Compatibility and rollout

- existing consumers:
- required/optional/nullable changes:
- enum or message changes:
- deployment order:
- mixed-version behaviour:
- deprecation:
- cross-repo blocker:

## Contract tests

| Scenario | Request/actor | Expected status/message/body |
|---|---|---|
| | | |
